'use strict'

/*
sacr la media de tres numeros dados por el usuario
*/

var uno = parseInt(prompt("Escriba el primer numero: "));
var dos = parseInt(prompt("Escriba el segundo numero: "));
var tres = parseInt(prompt("Escriba el tercer numero: "));

var media = (uno + dos + tres)/3;

console.log("La media es: "+media);